<?xml version="1.0" encoding="utf-8"?>

<!-- TODO: This could be genericized by removing android:text so the caller
     can set it dynamically. -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
   android:layout_width="wrap_content"
   android:layout_height="match_parent"
   android:gravity="center" style="@style/Widget.AppCompat.ActionButton"
   android:text="@string/slide_menu_barcode_scanner_short"
   android:drawableLeft="@drawable/ic_barcode_black"
   android:background="?attr/selectableItemBackground"
   android:clickable="true"
   android:textSize="12sp"
   android:textStyle="bold"
   android:textColor="?attr/actionMenuTextColor"
   android:textAllCaps="true" />
